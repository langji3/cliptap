# 一键更新验证

- Release 构建通过，0 警告、0 错误；32 项测试通过。
- 更新测试覆盖安装版与便携版隔离、版本比较、发布校验、网络异常、取消，以及固定仓库下载地址、下载成功、错误 SHA-256、长度不符和大小上限。
- 使用虚构字节作为下载响应，不执行任何安装包。
- 检查包含空格与单引号的自定义 D 盘安装目录被正确传入；使用 PowerShell Parser 验证辅助脚本语法，不执行辅助脚本。
- 安装器编译与发布包校验在发布前完成。按用户要求不实际安装，因此静默安装和重启链路未做端到端实机验证。
