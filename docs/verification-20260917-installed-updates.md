# 安装版检查更新与界面修正验证

## 当前交付：EXE 安装向导

用户随后明确要求双击 Setup.exe 分步安装，不创建任何快捷方式，完成后默认后台运行；并要求由用户自行安装，停止安装测试。

- 当前产物为 `artifacts/ClipTap-win-x64-setup.exe`，已替代早先的脚本 ZIP。Inno Setup 中文向导包含欢迎、目录选择、安装确认、进度和完成页；默认勾选 `--background` 运行，静默模式不启动。
- 不定义快捷方式；默认当前用户安装，无管理员权限，内置 .NET 运行时。安装/升级需先退出当前托盘进程；卸载保留用户库。
- EXE、安装包及新用户默认图标改为浅蓝色 `#6CA5E8`。已有用户主题色保持不变。
- 最终源码构建成功，0 警告/错误，30 项应用测试通过；安装器编译成功。只读独立审查确认无快捷方式、默认后台运行、发行类型与更新附件名称一致，未发现确定问题。
- 用户要求停止前，独立测试 AppId 的旧构建已通过 EXE 安装、覆盖升级、运行时及卸载登记检查，并已卸载。用户要求停止后，未再运行任何安装测试或启动 Setup；最终浅蓝图标版本由用户自行验证向导与安装体验。
- 曾尝试读取测试向导窗口但没有获得可捕获窗口，因此未将向导截图/视觉验收记录为通过。此前 ZIP 记录仅为历史阶段。

## 结果

- Portable 和 Installed 构建均通过默认 30 项测试。覆盖便携版零网络请求、版本数字比较、旧版/相同版、新稳定版、草稿/预发布、无 Release、无安装资产、非法版本、非 JSON、HTTP 拒绝、断网和取消。Release URL 由固定仓库地址生成，不使用远程任意链接。
- 安装版设置页渲染已检查，版本与检查按钮可见；便携版不显示该区域。发布输出均为 Windows x64 自包含构建。
- 隔离安装测试通过：初次安装、重新安装、模拟集成失败后的旧版本备份保留、重试、卸载及用户库保留。未改动真实开始菜单、卸载登记或自启动；这些系统集成仍需真实安装复核。
- 两名只读审查代理分别审查更新服务/UI 和安装脚本。更新服务无确认问题；安装审查发现失败后备份清理过早，已改为集成完成才清理，增加模拟失败验证。失败时保留旧目录供恢复，未实现快捷方式/注册表的全事务回滚。
- 去掉系统默认虚线焦点装饰，保留自定义主题色焦点提示；居中标题剪贴板图形、返回箭头和更多按钮，重建托盘共用的图标资源。检查了列表与编辑器渲染。
- 当前运行的便携版已从本地构建更新并后台重启，程序集与发布暂存目录哈希一致。

## 分发和未完成确认

- GitHub Release 查询为空；远程 Actions 成功运行提供现有轻量版构建产物。本次代码和新安装包尚未上传、未创建公开 Release。
- 本地安装包：`artifacts/ClipTap-win-x64-setup.zip`；便携包：`artifacts/ClipTap-win-x64-portable.zip`。
- 用户再次报告替换偶发失效。后续元数据采样时前台已分别是 Edge / IntelliJ、输入法转换模式 1025，不能代表发生问题时的 Codex 状态。未复现本次偶发问题，不能宣称其根因已解决。界面测试会短暂切换前台焦点；测试结束并重启后需用户再次确认。
