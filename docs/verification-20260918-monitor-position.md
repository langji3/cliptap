# 多屏定位修复

诊断确认：桌面、IDEA 和 Edge 均收到快捷键，窗口显示及激活成功。问题在定位阶段混用目标窗口 DPI、屏幕坐标与 WPF DIPs。诊断版改用 SetWindowPos 和 GetWindowRect，在目标监视器内按实际像素尺寸定位后，用户反馈“有了”。

正式实现保留此定位修复，移除 HotkeyDiagnostics、诊断参数入口、临时状态日志，恢复正式数据目录逻辑。诊断进程已关闭，未修改正式安装目录或用户数据。

回归包含主副屏坐标、负坐标、缩放后窗口尺寸及工作区不足的定位。完整构建与 34 项测试通过；安装留给用户进行。
