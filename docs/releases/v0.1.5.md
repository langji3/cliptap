修复多显示器、不同缩放比例下按 Alt+空格后看不到浮窗的问题。

- 根因是屏幕像素坐标与 WPF 布局坐标混用，导致窗口被放到错误位置；并非 Edge 或 IDEA 独有的快捷键冲突。
- 改用原生屏幕坐标定位，在目标显示器上完成 DPI 切换后按实际窗口尺寸居中显示。
- 用户已在此前无法唤出的场景中验证修复有效；构建与 34 项测试通过。
- 正式版移除临时诊断日志及隔离测试入口。

v0.1.2 及以上安装版可在设置中点击“检查更新”→“立即更新”。更新保留原安装目录、片段及设置。更早版本需先退出托盘程序，再手动安装最新版。

安装版：`ClipTap-win-x64-setup.exe`；便携版：`ClipTap-win-x64-portable.zip`。均自带运行时，校验值见 `SHA256SUMS.txt`。
