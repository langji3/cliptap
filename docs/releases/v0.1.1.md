修复触发词输入错误后，使用退格删除并重新输入无法自动替换的问题。

- 支持删除部分正确前缀后重新输入。
- 支持删掉多个错字后继续完成触发词。
- 支持长按退格连续删除。
- 导航、切换焦点、组合删除和超时仍会取消匹配；不读取输入框内容，不保存错字文本。

请先从托盘退出旧版，再运行安装包覆盖升级。已有片段和设置会保留。

安装版下载 `ClipTap-win-x64-setup.exe`；免安装用户下载并解压 `ClipTap-win-x64-portable.zip`。安装版也可在设置中点击“检查更新”前往下载。两个包均自带运行时，校验值见 `SHA256SUMS.txt`。
